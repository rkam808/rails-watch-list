<%= simple_form_for [@list, @movie, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.input :movie_id, collection: Movie.all %>
  <%= f.submit "Submit this Bookmark", class: "btn btn-primary" %>
<% end %>
